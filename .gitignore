## python
**/.venv
**/__pycache__
**/.pytest_cache

## vector store
**/qdrant_storage

# Local environment variables and keys
.env
.env.local
.env.*.local
*.key
*.pem
arena_votes*.jsonl
arena_user_votes.jsonl

## Local dev
outputs

## tmp
tmp
.DS_Store
db

## Failed Transcripts
FailedTranscripts.xlsx

## Arena runtime data (do not commit)
src/openwebui/data/*.jsonl

## Arena Admin tools (contain API keys)
arena_live.py
/tmp/arena_*.html
src/openwebui/data/*.backup*
src/openwebui/arena_comparisons.json
data/*.jsonl
data/*.backup*

# ============================================================================
# SECURITY: Private Keys & Certificates
# ============================================================================
# SSH Keys
**/.ssh/
id_rsa
id_rsa.pub
id_dsa
id_ecdsa
id_ed25519

# SSL/TLS Private Keys (certificates are auto-generated, never commit)
# Include BACKUPS - even .bak files contain sensitive keys!
nginx/ssl/
nginx/ssl-local/
**/privkey.pem
**/privkey.pem.bak*
**/private.key
**/fullchain.pem*
*.key
*.pem

# PGP/GPG Keys
secring.*

# AWS/Azure/GCP Credentials
.aws/credentials
.azure/
.gcp/

# Never commit .env files with secrets
.env.production
.env.local
.env.*.local
.env.production.demo

voting_ui.log
voting_ui.pid

# VM reference (local/private)
docs/VM-REQUIREMENTS.md

# Archive docs (local/private)
docs/archive/

# Local archive folder (potentially sensitive)
archive/

# Local-only demo notes
docs/SELF_SIGNED_TLS_DEMO.md

